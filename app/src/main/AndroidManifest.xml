<?xml version="1.0" encoding="utf-8"?>
<!--
  ============================================================
   AndroidManifest.xml  —  a "carteira de identidade" do app
  ============================================================
   Diz ao sistema Android: como o app se chama, qual ícone usar,
   qual tela abre primeiro e quais permissões ele precisa.
  ============================================================
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- O app precisa de internet para falar com o Firebase (login e banco na nuvem). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Listinha">

        <!--
          A Activity é uma "tela" do app. MainActivity é a principal.
          O intent-filter com MAIN + LAUNCHER significa:
          "esta é a tela que abre quando o usuário toca no ícone".
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Listinha.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
